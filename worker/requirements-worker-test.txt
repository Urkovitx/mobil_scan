# Minimal requirements for worker test image
# This is a lightweight version to test the build process

# Core dependencies
paddlepaddle==3.0.0b1

# Basic utilities
numpy==1.24.3
pillow==10.0.0
